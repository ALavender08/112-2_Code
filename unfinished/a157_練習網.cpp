#include <bits/stdc++.h>
using namespace std;

int fib[31]={0,1}; //全域變數初始化後後面全部預設0

int F(int n)
{
    if (n==0 || n==1)
        return fib[n];
    else 
    {
        
    }
        return F(n-1)+F(n-2);
}

int main()
{
    0
}
